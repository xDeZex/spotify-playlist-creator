# No spec changes

This change has no new or modified capabilities. The behavioral requirements in
`openspec/specs/create-album-playlists/spec.md` are unchanged — the endpoint swap
from `/users/{user_id}/playlists` to `/me/playlists` is a pure implementation detail.
